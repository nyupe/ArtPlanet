<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper    
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"    
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mybatis.Android">

	<select id="androidSelectListGCS" parameterType="java.util.Map" resultType="java.util.Map">
		select title,content,fileurl,nickname,categorie 
			From BLOGPOST b JOIN GCS g ON b.memberno = g.memberNo JOIN member m ON
				b.memberNo = m.memberNo order by postdate DESC
	</select>
</mapper>